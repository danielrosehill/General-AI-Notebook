List all saved outputs organized by date.

1. List all date subdirectories in `outputs/`.
2. For each date, list the output files with their titles (from the first heading in each file).
3. Show total count of outputs.

If no outputs exist yet, report that.
