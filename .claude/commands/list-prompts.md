List all pending prompts in the queue.

1. List all files in `prompts/to-run/`.
2. For each file, show:
   - Filename
   - File type (text or audio)
   - Brief preview (first ~100 chars for text files, or "Audio file - needs transcription" for audio)
3. Show total count of pending prompts.

If the directory is empty, report that there are no pending prompts.
